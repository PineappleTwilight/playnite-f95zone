<UserControl x:Class="F95ZoneMetadataProvider.F95ZoneMetadataProviderSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:f95ZoneMetadataProvider="clr-namespace:F95ZoneMetadataProvider"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600" d:DataContext="{d:DesignInstance f95ZoneMetadataProvider:Settings}">

    <UserControl.Resources>
        <ObjectDataProvider x:Key="DataFromEnum" ObjectType="{x:Type system:Enum}" MethodName="GetValues">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="f95ZoneMetadataProvider:PlayniteProperty" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <f95ZoneMetadataProvider:IntToStringConverter x:Key="IntToStringConverter" />
    </UserControl.Resources>

    <StackPanel>

        <TextBlock TextWrapping="Wrap" Text="Use this login button to log into F95zone." Margin="8, 4, 8, 0" />
        <Button Click="ButtonBase_OnClick" Content="Login" Margin="8, 0, 8, 0" />

        <TextBlock Text="Number Of Registered Cookies:" Margin="8, 4, 8, 0" />
        <TextBox Text="{Binding CookiesCount, Mode=OneWay}" IsReadOnly="True" Margin="8, 0, 8, 0" />

        <TextBlock Text="Which property should the F95zone labels be assigned to in Playnite:" Margin="8, 4, 8, 0" />
        <ComboBox ItemsSource="{Binding Source={StaticResource DataFromEnum}}"
                    SelectedItem="{Binding LabelProperty}" Margin="8, 0, 8, 0" />

        <TextBlock Text="Which property should the F95zone tags be assigned to in Playnite:" Margin="8, 4, 8, 0" />
        <ComboBox ItemsSource="{Binding Source={StaticResource DataFromEnum}}"
                    SelectedItem="{Binding TagProperty}" Margin="8, 0, 8, 0" />

        <TextBlock Text="Check for updates on startup:" Margin="8, 4, 8, 0" />
        <CheckBox IsChecked="{Binding CheckForUpdates, Mode=TwoWay}" Margin="8, 0, 8, 0" />

        <TextBlock Text="Scrape misc. game links:" Margin="8, 4, 8, 0" />
        <CheckBox IsChecked="{Binding ShouldScrapeLinks, Mode=TwoWay}" Margin="8, 0, 8, 0" />

        <TextBlock Text="Look for updates of finished/completed games:" Margin="8, 4, 8, 0" />
        <CheckBox IsChecked="{Binding UpdateFinishedGames, Mode=TwoWay}" Margin="8, 0, 8, 0" />

        <TextBlock Text="Use the default F95zone icon for games:" Margin="8, 4, 8, 0" />
        <CheckBox IsChecked="{Binding SetDefaultIcon, Mode=TwoWay}" Margin="8, 0, 8, 0" />
    </StackPanel>
</UserControl>